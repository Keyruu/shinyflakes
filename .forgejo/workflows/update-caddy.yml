on:
  push:
    paths:
      - nix/packages/caddy.nix
      - flake.lock

jobs:
  update-caddy:
    runs-on: nixos-x86_64-linux
    uses: ./.forgejo/workflows/update-hash.yml
    with:
      package: caddy
    secrets: inherit